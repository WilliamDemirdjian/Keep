<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/backgroundLight</item>
        <item name="colorPrimaryDark">@color/backgroundLight</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="darkTheme">
        <item name="backgroundTint">@color/backgroundDark</item>
        <item name="android:textColor">@color/textColorBackgroundDark</item>
    </style>
</resources>
